This app has been produced under Vue.js framework.
It uses VueX to manage the states
It also uses Bootstrap to render the css. So if you are downloading this app be carefull to have the right enviroment setup.

The application allows the user to create, delete and update notes. All queries are made to an online API.
